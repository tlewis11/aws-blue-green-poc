pipeline {
    agent any
    //parameters {
    //    string(name: 'AWS_ACCESS_KEY_ID', description: 'AWS Access Key')
    //    string(name: 'AWS_SECRET_ACCESS_KEY', description: 'AWS Secret Key')
    //}
    stages {
      stage('deploy cloudformation stack') {
        steps {
          echo 'placeholder: deploying to aws via cfn...'
        }
      }
    }
}
